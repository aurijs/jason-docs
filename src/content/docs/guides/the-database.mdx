---
    title: The JasonDB Class
    description: Base class
---

## The Base Class

Everything starts with the `JasonDB` class.

```typescript
const db = new JasonDB();
```

With this, you can now create a new database. By default it will
automatically save.